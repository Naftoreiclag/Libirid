#ifndef WORDMODIFIER_H
#define WORDMODIFIER_H

#include <vector>
#include <string>

class WordModifier
{
    public:
        WordModifier(std::string form);
    private:
        std::string form;
};

#endif // WORDMODIFIER_H
